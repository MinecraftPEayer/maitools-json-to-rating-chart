import BookmarkletPage from "./BookmarkletPage";

export const metadata = {
    title: "maibot-bookmarklet",
    description: "Bookmarklet script for maibot",
};

const Page = () => {
    return <BookmarkletPage />;
};

export default Page;
